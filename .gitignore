node_modules/
dist/
dist-types/
build/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
*.log
npm-debug.log*

.vite/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Source card scans: 90-255 MB per file, which GitHub rejects outright (its
# hard limit is 100 MB per file). Keep them locally, or track them with Git LFS
# if they need to be shared. The cut cards in art-assets/cards/ are committed.
art-assets/PDFs/

# Intermediate glyph-matching data, regenerated by scripts/cluster-glyphs.py.
scripts/glyph-clusters.json
scripts/card-stats.json
